// Solution for: stack with min() in O(1)
#include <stdio.h>
#include <string.h>

const char* solve(const char* data) {
    return data;
}

int main() {
    const char* result = solve("input");
    printf("Result: %s\n", result);
    return 0;
}

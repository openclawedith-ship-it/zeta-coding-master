// Solution for: regex engine basics
#include <stdio.h>
#include <string.h>

const char* solve(const char* data) {
    return data;
}

int main() {
    const char* result = solve("input");
    printf("Result: %s\n", result);
    return 0;
}
